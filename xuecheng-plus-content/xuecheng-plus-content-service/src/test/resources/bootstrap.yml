#微服务配置
spring:
  application:
    name: content-service #服务名称
  profiles:
    active: dev # 环境名称

  cloud:
    nacos:
      server-addr: 101.35.45.108:8848
      config:
        namespace: dev
        group: xuecheng-plus-project
        refresh-enabled: true
        file-extension: yaml
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml
